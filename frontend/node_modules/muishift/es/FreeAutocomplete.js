var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

import MuiShift from './MuiShift';
import React from 'react';
import TextField from '@material-ui/core/TextField';

var renderInput = function renderInput(_ref) {
  var rootProps = _ref.rootProps,
      downshiftProps = _ref.downshiftProps;
  var getInputProps = downshiftProps.getInputProps,
      inputValue = downshiftProps.inputValue,
      selectedItem = downshiftProps.selectedItem,
      input = downshiftProps.input,
      selectItem = downshiftProps.selectItem,
      clearSelection = downshiftProps.clearSelection;
  var inputProps = rootProps.inputProps;


  return React.createElement(TextField, _extends({}, inputProps, {
    InputProps: _extends({}, getInputProps({
      onBlur: function onBlur(e) {
        e.preventDefault();

        if (input) {
          input.onBlur(e);
        }

        selectItem(e.target.value);
      }
    }))
  }));
};

var FreeAutocomplete = function FreeAutocomplete(props) {
  var input = props.input;


  return React.createElement(MuiShift, _extends({}, props, {
    renderInput: renderInput,
    onStateChange: function onStateChange(_ref2) {
      var inputValue = _ref2.inputValue;

      return input && input.onChange ? input.onChange(inputValue) : undefined;
    }
  }));
};

export default FreeAutocomplete;